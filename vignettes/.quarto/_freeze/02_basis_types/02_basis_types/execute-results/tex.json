{
  "hash": "01ec7d396f74b75193e0cd9efb5b367b",
  "result": {
    "engine": "julia",
    "markdown": "---\ntitle: \"Comparing Smooth Basis Types\"\nauthor: \"GAM.jl Contributors\"\nformat:\n  html:\n    code-fold: false\n    toc: true\n  gfm:\n    toc: true\n  pdf:\n    toc: true\nengine: julia\n---\n\n## Overview\n\nGAMs represent smooth functions as linear combinations of **basis functions**. The choice of basis affects the shape of the fitted smooth, computational cost, and numerical properties. GAM.jl supports several basis types:\n\n| Symbol | Basis | Description |\n|--------|-------|-------------|\n| `:tp` | Thin plate regression spline | Default. Optimal in a certain sense; no knot placement needed |\n| `:cr` | Cubic regression spline | Cubic spline with knots at data quantiles; efficient for 1D |\n| `:ps` | P-spline | B-spline basis with difference penalty |\n| `:gp` | Gaussian process | Squared-exponential covariance as a basis |\n\nThis vignette fits the same data with each basis and compares the results.\n\n## Setup\n\n::: {.cell execution_count=1}\n``` {.julia .cell-code}\nusing GAM\nusing StatsAPI: deviance, r2\n\nusing DataFrames\nusing Random\nusing Plots\n```\n:::\n\n\n\n## Simulating data\n\nWe simulate $n = 300$ observations from a function with both broad and fine-scale structure:\n\n$$\ny_i = \\sin(2\\pi x_i) + 0.5\\sin(6\\pi x_i) + \\varepsilon_i, \\quad \\varepsilon_i \\sim \\mathcal{N}(0, 0.5^2)\n$$\n\n::: {.cell execution_count=1}\n``` {.julia .cell-code}\nRandom.seed!(42)\nn = 300\nx = sort(rand(n))\nf_true = sin.(2π .* x) .+ 0.5 .* sin.(6π .* x)\ny = f_true .+ 0.5 .* randn(n)\ndf = DataFrame(x = x, y = y)\n```\n\n::: {.cell-output .cell-output-display execution_count=1}\n```{=tex}\n\\begin{tabular}{r|cc}\n\t& x & y\\\\\n\t\\hline\n\t& Float64 & Float64\\\\\n\t\\hline\n\t1 & 0.000715255 & 0.654687 \\\\\n\t2 & 0.00119567 & 0.0229512 \\\\\n\t3 & 0.0100473 & 0.0491133 \\\\\n\t4 & 0.016095 & 0.540544 \\\\\n\t5 & 0.0269848 & -0.0270783 \\\\\n\t6 & 0.0301856 & 0.202606 \\\\\n\t7 & 0.0369673 & 1.16376 \\\\\n\t8 & 0.0379955 & 0.538741 \\\\\n\t9 & 0.040211 & 0.76023 \\\\\n\t10 & 0.0424826 & 0.803596 \\\\\n\t11 & 0.0467336 & -0.0516383 \\\\\n\t12 & 0.0467959 & 0.147176 \\\\\n\t13 & 0.0484837 & 1.15456 \\\\\n\t14 & 0.0513185 & 0.381877 \\\\\n\t15 & 0.0552982 & 0.404848 \\\\\n\t16 & 0.055968 & 1.10396 \\\\\n\t17 & 0.0588083 & 0.783381 \\\\\n\t18 & 0.0589488 & 1.7587 \\\\\n\t19 & 0.0604437 & 0.737216 \\\\\n\t20 & 0.0651857 & 1.35085 \\\\\n\t21 & 0.066672 & 1.58092 \\\\\n\t22 & 0.0726606 & -0.0850994 \\\\\n\t23 & 0.0786647 & 1.31219 \\\\\n\t24 & 0.0793339 & 1.9222 \\\\\n\t$\\dots$ & $\\dots$ & $\\dots$ \\\\\n\\end{tabular}\n```\n:::\n:::\n\n\n\n## Fitting models with different bases\n\nWe fit the same formula with each basis type, using `k = 20` basis functions:\n\n::: {.cell execution_count=1}\n``` {.julia .cell-code}\nbases = [:tp, :cr, :ps, :gp]\nmodels = Dict{Symbol, GamModel}()\n\nfor bs in bases\n    models[bs] = gam(GamFormula(:y, Symbol[], true, [s(:x, k=20, bs=bs)]), df)\nend\n```\n:::\n\n\n\n## Comparing EDF and deviance\n\n::: {.cell execution_count=1}\n``` {.julia .cell-code}\nprintln(\"Basis   EDF       Deviance    Dev.Expl(%)\")\nprintln(\"─\" ^ 50)\nfor bs in bases\n    m = models[bs]\n    e = round(edf(m)[1]; digits = 2)\n    d = round(deviance(m); digits = 2)\n    de = round(r2(m) * 100; digits = 1)\n    println(\"$(rpad(bs, 8))$(lpad(string(e), 8))  $(lpad(string(d), 10))  $(lpad(string(de), 10))\")\nend\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nBasis   EDF       Deviance    Dev.Expl(%)\n──────────────────────────────────────────────────\ntp         11.31       72.54        70.6\ncr         11.39       72.51        70.6\nps         11.01       73.05        70.4\ngp         13.47       71.49        71.0\n```\n:::\n:::\n\n\n\n## Comparing smooth estimates\n\nWe evaluate each smooth on the same grid and compare:\n\n::: {.cell execution_count=1}\n``` {.julia .cell-code}\np = plot(xlabel = \"x\", ylabel = \"f(x)\",\n    title = \"Smooth estimates by basis type\", legend = :topleft)\n\ncolors = [:steelblue, :darkorange, :green4, :purple]\n\nfor (i, bs) in enumerate(bases)\n    se = smooth_estimates(models[bs]; n = 200)\n    plot!(p, se.covariates[:x], se.estimate;\n        label = string(bs),\n        linewidth = 2,\n        color = colors[i])\nend\n\nplot!(p, x, f_true;\n    label = \"true f(x)\",\n    linestyle = :dash,\n    linewidth = 2,\n    color = :black)\np\n```\n\n::: {.cell-output .cell-output-display execution_count=1}\n![](02_basis_types_files/figure-pdf/cell-6-output-1.svg){fig-pos='H'}\n:::\n:::\n\n\n\nWe can also show each basis with its confidence band:\n\n::: {.cell execution_count=1}\n``` {.julia .cell-code}\nplots = []\nfor (i, bs) in enumerate(bases)\n    se = smooth_estimates(models[bs]; n = 200)\n    pi = plot(se.covariates[:x], se.estimate;\n        ribbon = 2 .* se.se,\n        fillalpha = 0.2,\n        label = string(bs),\n        linewidth = 2,\n        color = colors[i],\n        title = string(bs),\n        xlabel = \"x\",\n        ylabel = \"f(x)\")\n    plot!(pi, x, f_true;\n        label = \"truth\",\n        linestyle = :dash,\n        color = :black)\n    push!(plots, pi)\nend\nplot(plots...; layout = (2, 2), size = (800, 600))\n```\n\n::: {.cell-output .cell-output-display execution_count=1}\n![](02_basis_types_files/figure-pdf/cell-7-output-1.svg){fig-pos='H'}\n:::\n:::\n\n\n\n## Visualizing basis functions\n\nTo understand how each basis works, we can examine the model matrix columns for a small number of basis functions (`k = 8`):\n\n::: {.cell execution_count=1}\n``` {.julia .cell-code}\nplots_basis = []\nfor (i, bs) in enumerate(bases)\n    m_small = gam(GamFormula(:y, Symbol[], true, [s(:x, k=8, bs=bs)]), df)\n    X_smooth = m_small.smooths[1].X\n    k_cols = size(X_smooth, 2)\n    pi = plot(title = \"$(bs) basis (k=8)\", xlabel = \"x\", ylabel = \"basis value\",\n        legend = false)\n    for j in 1:k_cols\n        order = sortperm(df.x)\n        plot!(pi, df.x[order], X_smooth[order, j]; linewidth = 1)\n    end\n    push!(plots_basis, pi)\nend\nplot(plots_basis...; layout = (2, 2), size = (800, 600))\n```\n\n::: {.cell-output .cell-output-display execution_count=1}\n![](02_basis_types_files/figure-pdf/cell-8-output-1.svg){fig-pos='H'}\n:::\n:::\n\n\n\n## When to use which basis\n\n- **Thin plate (`:tp`)**: The default choice. Works well in any dimension. Optimal smoothness in a certain mathematical sense. Slightly more expensive than knot-based alternatives for large datasets.\n\n- **Cubic regression spline (`:cr`)**: Efficient for 1D smoothing with knots at data quantiles. Produces smooth curves that are natural cubic splines. Good default for univariate smooths.\n\n- **P-spline (`:ps`)**: B-spline basis with a difference penalty on adjacent coefficients. Evenly spaced knots. Computationally efficient and well-behaved, especially for evenly sampled data.\n\n- **Gaussian process (`:gp`)**: Uses a squared-exponential covariance kernel. Produces very smooth curves. Useful when the underlying function is believed to be infinitely differentiable.\n\n## Summary\n\nIn this vignette we:\n\n1. Simulated bumpy data with both low- and high-frequency components\n2. Fitted GAMs using four different basis types (TP, CR, PS, GP)\n3. Compared EDF, deviance, and smooth estimates across bases\n4. Visualized the raw basis functions for each type\n5. Discussed when each basis is most appropriate\n\nThe next vignette demonstrates models with multiple smooth terms.\n\n",
    "supporting": [
      "02_basis_types_files/figure-pdf"
    ],
    "filters": [],
    "includes": {}
  }
}